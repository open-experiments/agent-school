# Agent identity: the RCA agent runs under its own ServiceAccount (see
# 101-noc-assistant/deploy/ocp/serviceaccount.yaml for the rationale).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: rca-investigator
  labels:
    app.kubernetes.io/name: rca-investigator
    app.kubernetes.io/part-of: agent-school
