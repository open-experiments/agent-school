langgraph>=0.2.60
langchain-core>=0.3
# live mode extras:
#   langchain-openai   (the decide node's LLM; offline runs without it)
#   mlflow==3.4.0      (per-case audit runs; offline prints to stdout)
